---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: overlappingrangeipreservations.whereabouts.cni.cncf.io
spec:
  group: whereabouts.cni.cncf.io
  names:
    kind: OverlappingRangeIPReservation
    listKind: OverlappingRangeIPReservationList
    plural: overlappingrangeipreservations
    shortNames:
    - orip
    singular: overlappingrangeipreservation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.podref
      name: PodRef
      type: string
    - jsonPath: .spec.containerid
      name: ContainerID
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OverlappingRangeIPReservation is the Schema for the overlappingrangeipreservations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OverlappingRangeIPReservationSpec defines the desired state
              of OverlappingRangeIPReservation.
            properties:
              containerid:
                description: ContainerID is the identifier of the container that owns
                  this reservation.
                type: string
              ifname:
                description: IfName is the network interface name inside the pod for
                  this reservation.
                type: string
              podref:
                description: PodRef is the namespace/name reference of the pod that
                  owns this reservation.
                minLength: 1
                type: string
            required:
            - podref
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
